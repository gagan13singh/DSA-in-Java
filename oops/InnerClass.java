package oops;

public class InnerClass {
    static class Test{

    }
}

//Outside classes cant be static.
//Inner classes can or cannot be static.
//An inner class is a class defined inside another class.
// It helps logically group related classes and allows the inner class
// to access all members of the outer class, including private members.
// A static inner class can be created without creating an object
// of the outer class.